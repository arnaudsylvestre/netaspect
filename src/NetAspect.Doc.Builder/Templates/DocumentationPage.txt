$documentation.Header

<div class="container bs-docs-container">
      <div class="row">
      <div class="col-md-12" role="main">

$documentation.Basics

         <div class="bs-docs-section">
            <div class="page-header">
               <h1 id="dropdowns">Interceptors</h1>
            </div>
			<p>In NetAspect, an interceptor is a method which will be called </p>
			<h2 id="dropdowns">For method weaving</h2>
            <p>The "method weaving" will modify the code of the method to add behavior. It is possible to add behaviors :</p>
            <ul>
               <li><b>Before</b> the method is executed</li>
               <li><b>After</b> the method is executed when no exception is raised</li>
               <li><b>When exception occurs</b> in the method</li>
               <li><b>On finally</b> : at the end of the method when an exception is raised or not</li>
            </ul>
            <p>For each of these behaviors, we can get some information about the weaved method by adding some parameters to the aspect method.</p>
<input type="button" class="btn btn-primary" data-toggle="collapse" data-target="#MethodWeavingPossibilities" value="Show possibilities">
    <!-- Collapsible Element HTML -->
    <div id="MethodWeavingPossibilities" class="collapse">



#foreach ($possibility in ${documentation.MethodWeavings})

<!-- 
================================================	  
	  $possibility.Kind possibilities
================================================
	  -->

	<h4 id="dropdowns-example">$possibility.Title</h4>
            <p>These aspects are called when the aspect is put on a <b>$possibility.Member</b></p>
			
<div class="panel-group" id="Group$possibility.Kind">
#foreach ($event in $possibility.Events)
        <div class="panel panel-default">
            <div class="panel-heading">
                <h4 class="panel-title">
                    <a data-toggle="collapse" data-parent="#accordion" href="#$event.Kind">$event.Description</a>
                </h4>
            </div>
            <div id="$event.Kind" class="panel-collapse collapse">
                <div class="panel-body">
                    <p>The <b>$event.MethodName</b> method is called ${event.Called}. The available parameters are :</p>
            <ul>
#foreach ($parameter in $event.Parameters)
               <li><b>$parameter.Name</b> : $parameter.Description</li>
#end
            </ul>
			<p>Sample on how to use</p>
<pre class="prettyprint">
// The aspect
$event.Sample.AspectCode

// The class to weave
$event.Sample.ClassToWeaveCode

// The code which will call our aspect
$event.Sample.CallCode
</pre>
                </div>
            </div>
        </div>
#end
        
    </div>


#end
    </div>

			<h2 id="dropdowns">Instruction Weaving</h2>
            <h3 id="dropdowns-example">Theory in NetAspect</h3>
            <p>The "instruction weaving" will modify the code of the method which call the member with the aspect. It is possible to add behaviors :</p>
            <ul>
               <li><b>Before</b> the member is called</li>
               <li><b>After</b> the member is called</li>
            </ul>
            <p>For each of these behaviors, we can get some information about the caller and the member called by adding some parameters to the aspect method.</p>
<input type="button" class="btn btn-primary" data-toggle="collapse" data-target="#CallWeavingPossibilities" value="Show possibilities">
    <!-- Collapsible Element HTML -->
    <div id="CallWeavingPossibilities" class="collapse">



#foreach ($possibility in $documentation.InstructionWeavings)

<!-- 
================================================	  
	  $possibility.Kind possibilities
================================================
	  -->

	<h4 id="dropdowns-example">$possibility.Title</h4>
            <p>These aspects are called when the aspect is put on a <b>$possibility.Member</b></p>
			
<div class="panel-group" id="Group$possibility.Kind">
#foreach ($event in $possibility.Events)
        <div class="panel panel-default">
            <div class="panel-heading">
                <h4 class="panel-title">
                    <a data-toggle="collapse" data-parent="#accordion" href="#$event.Kind">$event.Description</a>
                </h4>
            </div>
            <div id="$event.Kind" class="panel-collapse collapse">
                <div class="panel-body">
                    <p>The <b>$event.MethodName</b> method is called ${event.Called}. The available parameters are :</p>
            <ul>
#foreach ($parameter in $event.Parameters)
               <li><b>$parameter.Name</b> : $parameter.Description</li>
#end
            </ul>
			<p>Sample on how to use</p>
<pre class="prettyprint">
// The aspect
$event.Sample.AspectCode

// The class to weave
$event.Sample.ClassToWeaveCode

// The code which will call our aspect
$event.Sample.CallCode
</pre>
                </div>
            </div>
        </div>
#end
        
    </div>


#end
    </div>











	<h2 id="dropdowns">Parameter Weaving</h2>
            <h3 id="dropdowns-example">Theory in NetAspect</h3>
            <p>The "parameter weaving" will modify the code of the method which is declared with a parameter with the aspect. It is possible to add behaviors :</p>
            <ul>
               <li><b>Before</b> the method is executed</li>
               <li><b>After</b> the method is executed when no exception is raised</li>
               <li><b>When exception occurs</b> in the method</li>
               <li><b>On finally</b> : at the end of the method when an exception is raised or not</li>
            </ul>
            </ul>
            <p>For each of these behaviors, we can get some information by adding some parameters to the aspect method.</p>
<input type="button" class="btn btn-primary" data-toggle="collapse" data-target="#ParameterWeavingPossibilities" value="Show possibilities">
    <!-- Collapsible Element HTML -->
    <div id="ParameterWeavingPossibilities" class="collapse">



#foreach ($possibility in $documentation.ParameterWeavings)

<!-- 
================================================	  
	  $possibility.Kind possibilities
================================================
	  -->

	<h4 id="dropdowns-example">$possibility.Title</h4>
            <p>These aspects are called when the aspect is put on a <b>$possibility.Member</b></p>
			
<div class="panel-group" id="Group$possibility.Kind">
#foreach ($event in $possibility.Events)
        <div class="panel panel-default">
            <div class="panel-heading">
                <h4 class="panel-title">
                    <a data-toggle="collapse" data-parent="#accordion" href="#$event.Kind">$event.Description</a>
                </h4>
            </div>
            <div id="$event.Kind" class="panel-collapse collapse">
                <div class="panel-body">
                    <p>The <b>$event.MethodName</b> method is called ${event.Called}. The available parameters are :</p>
            <ul>
#foreach ($parameter in $event.Parameters)
               <li><b>$parameter.Name</b> : $parameter.Description</li>
#end
            </ul>
			<p>Sample on how to use</p>
<pre class="prettyprint">
// The aspect
$event.Sample.AspectCode

// The class to weave
$event.Sample.ClassToWeaveCode

// The code which will call our aspect
$event.Sample.CallCode
</pre>
                </div>
            </div>
        </div>
#end
        
    </div>


#end
    </div>



















         </div>
</div>